name: wetherapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.3.2 <4.0.0'

flutter:
  assets:
    - assets/sky.jpg

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.2
  cupertino_icons: ^1.0.6
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

uses-material-design: true
